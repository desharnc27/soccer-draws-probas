/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package garbage;

/**
 *
 * @author desharnc27
 */
public class SpookyNode {

    /*byte[] permutation;

    public void spookOrder() {
        for (int i = 1; i < Statix.NB_GROUPS; i++) {
            for (int j = i; j < Statix.NB_GROUPS; j++) {
                if (koopa(i, j) > 0) {
                    biSwap(i, j);
                }
            }
        }
    }

    private void biSwap(int c0, int c1) {
        int nbRows = potsState.length;
        for (int i = 0; i < nbRows; i++) {
            byte temp = potsState[i][c0];
            potsState[i][c0] = potsState[i][c1];
            potsState[i][c1] = temp;
        }
        byte temp = permutation[c0];
        permutation[c0] = permutation[c1];
        permutation[c1] = temp;
    }*/
}
